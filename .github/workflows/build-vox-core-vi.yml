name: Build VOX Core VI

on:
  push:
    paths:
      - 'modules/VOXCoreVI/**'
      - '.github/workflows/build-vox-core-vi.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Fetch SHVDNE reference
        shell: pwsh
        run: |
          $url = 'https://github.com/Chiheb-Bacha/ScriptHookVDotNetEnhanced/releases/download/v1.1.0.6/ScriptHookVDotNetEnhanced-v1.1.0.6.zip'
          Invoke-WebRequest -Uri $url -OutFile shvdne.zip
          Expand-Archive shvdne.zip -DestinationPath shvdne
          $ref = Get-ChildItem shvdne -Recurse -Filter ScriptHookVDotNet3.dll | Select-Object -First 1
          if (-not $ref) { throw 'ScriptHookVDotNet3.dll not found in SHVDNE archive.' }
          New-Item -ItemType Directory -Force modules/VOXCoreVI/refs | Out-Null
          Copy-Item $ref.FullName modules/VOXCoreVI/refs/ScriptHookVDotNet3.dll
      - name: Build
        shell: pwsh
        run: dotnet build modules/VOXCoreVI/VOXCoreVI.csproj -c Release
      - name: Package
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force dist/scripts | Out-Null
          Copy-Item modules/VOXCoreVI/bin/Release/net48/VOXCoreVI.dll dist/scripts/VOXCoreVI.dll
          Copy-Item modules/VOXCoreVI/INSTALL.txt dist/INSTALL.txt
          Compress-Archive -Path dist/* -DestinationPath VOXCoreVI-v0.1.0.zip -Force
      - name: Upload test build
        uses: actions/upload-artifact@v4
        with:
          name: VOXCoreVI-v0.1.0
          path: VOXCoreVI-v0.1.0.zip
          if-no-files-found: error
